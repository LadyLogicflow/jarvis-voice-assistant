# Core web stack
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
websockets>=13.0
httpx>=0.27.0

# LLM / TTS
anthropic>=0.39.0

# Browser automation + screen vision
playwright>=1.40.0
Pillow>=10.0.0

# Audio (clap + wakeword triggers)
sounddevice>=0.4.7
numpy>=1.26.0
pvporcupine>=3.0.0

# Google Calendar integration
google-api-python-client>=2.140.0
google-auth>=2.34.0
google-auth-oauthlib>=1.2.0

# Natural-language date parsing for [ACTION:ADDCAL]
dateparser>=1.2.0

# Configuration / secrets loading from .env
python-dotenv>=1.0.0

# Retry helper for transient API failures (Anthropic, ElevenLabs, wttr.in)
tenacity>=8.2

# Telegram bot channel (issue #47)
python-telegram-bot>=21.0
faster-whisper>=1.0  # local STT, no PyTorch / no API key

# IMAP IDLE push monitor (issue #48) — async, supports IDLE
aioimaplib>=1.0

# Semantisches Gedächtnis (issue #57) — Vektorspeicher + Embeddings
chromadb>=0.5.0
sentence-transformers>=3.0.0
