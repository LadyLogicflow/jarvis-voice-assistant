# Dev-only dependencies. Install with:
#   python3 -m pip install -r requirements.txt -r requirements-dev.txt

pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0
respx>=0.21          # httpx mock backend
ruff>=0.6
